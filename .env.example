# Neuron stores its AI provider configuration (base URL, model, API key) inside
# the app via Settings → AI Provider. The API key is encrypted with Electron's
# safeStorage and persisted in the local SQLite database — not in this file.
#
# This file exists only for environment-level overrides used during development.
NODE_ENV=development
